---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dashboards.checkly.checkly.crossplane.io
spec:
  group: checkly.checkly.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - checkly
    kind: Dashboard
    listKind: DashboardList
    plural: dashboards
    singular: dashboard
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Dashboard is the Schema for the Dashboards API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardSpec defines the desired state of Dashboard
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  checksPerPage:
                    description: |-
                      (Number) Determines how many checks to show per page. Possible values are between 1 and 20. (Default 15).
                      Determines how many checks to show per page. Possible values are between 1 and 20. (Default `15`).
                    type: number
                  customCss:
                    description: |-
                      (String) Custom CSS to be applied to the dashboard.
                      Custom CSS to be applied to the dashboard.
                    type: string
                  customDomain:
                    description: |-
                      (String) A custom user domain, e.g. 'status.example.com'. See the docs on updating your DNS and SSL usage.
                      A custom user domain, e.g. 'status.example.com'. See the docs on updating your DNS and SSL usage.
                    type: string
                  customUrl:
                    description: |-
                      (String) A subdomain name under 'checklyhq.com'. Needs to be unique across all users.
                      A subdomain name under 'checklyhq.com'. Needs to be unique across all users.
                    type: string
                  description:
                    description: |-
                      (String) HTML  description for the dashboard.
                      HTML <meta> description for the dashboard.
                    type: string
                  enableIncidents:
                    description: |-
                      (Boolean) Enable incident support for the dashboard. (Default false).
                      Enable incident support for the dashboard. (Default `false`).
                    type: boolean
                  expandChecks:
                    description: |-
                      (Boolean) Expand or collapse checks on the dashboard. (Default false).
                      Expand or collapse checks on the dashboard. (Default `false`).
                    type: boolean
                  favicon:
                    description: |-
                      (String) A URL pointing to an image file to use as browser favicon.
                      A URL pointing to an image file to use as browser favicon.
                    type: string
                  header:
                    description: |-
                      (String) A piece of text displayed at the top of your dashboard.
                      A piece of text displayed at the top of your dashboard.
                    type: string
                  hideTags:
                    description: |-
                      (Boolean) Show or hide the tags on the dashboard. (Default false).
                      Show or hide the tags on the dashboard. (Default `false`).
                    type: boolean
                  isPrivate:
                    description: |-
                      (Boolean) Set your dashboard as private and generate key.
                      Set your dashboard as private and generate key.
                    type: boolean
                  link:
                    description: |-
                      (String) A link to for the dashboard logo.
                      A link to for the dashboard logo.
                    type: string
                  logo:
                    description: |-
                      (String) A URL pointing to an image file to use for the dashboard logo.
                      A URL pointing to an image file to use for the dashboard logo.
                    type: string
                  paginate:
                    description: |-
                      (Boolean) Determines if pagination is on or off. (Default true).
                      Determines if pagination is on or off. (Default `true`).
                    type: boolean
                  paginationRate:
                    description: |-
                      (Number) How often to trigger pagination in seconds. Possible values 30, 60 and 300. (Default 60).
                      How often to trigger pagination in seconds. Possible values `30`, `60` and `300`. (Default `60`).
                    type: number
                  refreshRate:
                    description: |-
                      (Number) How often to refresh the dashboard in seconds. Possible values 60, '300' and 600. (Default 60).
                      How often to refresh the dashboard in seconds. Possible values `60`, '300' and `600`. (Default `60`).
                    type: number
                  showCheckRunLinks:
                    description: |-
                      (Boolean) Show or hide check run links on the dashboard. (Default false).
                      Show or hide check run links on the dashboard. (Default `false`).
                    type: boolean
                  showHeader:
                    description: |-
                      (Boolean) Show or hide header and description on the dashboard. (Default true).
                      Show or hide header and description on the dashboard. (Default `true`).
                    type: boolean
                  showP95:
                    description: |-
                      (Boolean) Show or hide the P95 stats on the dashboard. (Default true).
                      Show or hide the P95 stats on the dashboard. (Default `true`).
                    type: boolean
                  showP99:
                    description: |-
                      (Boolean) Show or hide the P99 stats on the dashboard. (Default true).
                      Show or hide the P99 stats on the dashboard. (Default `true`).
                    type: boolean
                  tags:
                    description: |-
                      (Set of String) A list of one or more tags that filter which checks to display on the dashboard.
                      A list of one or more tags that filter which checks to display on the dashboard.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  useTagsAndOperator:
                    description: |-
                      (Boolean) Set when to use AND operator for fetching dashboard tags. (Default false).
                      Set when to use AND operator for fetching dashboard tags. (Default `false`).
                    type: boolean
                  width:
                    description: |-
                      (String) Determines whether to use the full screen or focus in the center. Possible values are FULL and 960PX. (Default FULL).
                      Determines whether to use the full screen or focus in the center. Possible values are `FULL` and `960PX`. (Default `FULL`).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  checksPerPage:
                    description: |-
                      (Number) Determines how many checks to show per page. Possible values are between 1 and 20. (Default 15).
                      Determines how many checks to show per page. Possible values are between 1 and 20. (Default `15`).
                    type: number
                  customCss:
                    description: |-
                      (String) Custom CSS to be applied to the dashboard.
                      Custom CSS to be applied to the dashboard.
                    type: string
                  customDomain:
                    description: |-
                      (String) A custom user domain, e.g. 'status.example.com'. See the docs on updating your DNS and SSL usage.
                      A custom user domain, e.g. 'status.example.com'. See the docs on updating your DNS and SSL usage.
                    type: string
                  customUrl:
                    description: |-
                      (String) A subdomain name under 'checklyhq.com'. Needs to be unique across all users.
                      A subdomain name under 'checklyhq.com'. Needs to be unique across all users.
                    type: string
                  description:
                    description: |-
                      (String) HTML  description for the dashboard.
                      HTML <meta> description for the dashboard.
                    type: string
                  enableIncidents:
                    description: |-
                      (Boolean) Enable incident support for the dashboard. (Default false).
                      Enable incident support for the dashboard. (Default `false`).
                    type: boolean
                  expandChecks:
                    description: |-
                      (Boolean) Expand or collapse checks on the dashboard. (Default false).
                      Expand or collapse checks on the dashboard. (Default `false`).
                    type: boolean
                  favicon:
                    description: |-
                      (String) A URL pointing to an image file to use as browser favicon.
                      A URL pointing to an image file to use as browser favicon.
                    type: string
                  header:
                    description: |-
                      (String) A piece of text displayed at the top of your dashboard.
                      A piece of text displayed at the top of your dashboard.
                    type: string
                  hideTags:
                    description: |-
                      (Boolean) Show or hide the tags on the dashboard. (Default false).
                      Show or hide the tags on the dashboard. (Default `false`).
                    type: boolean
                  isPrivate:
                    description: |-
                      (Boolean) Set your dashboard as private and generate key.
                      Set your dashboard as private and generate key.
                    type: boolean
                  link:
                    description: |-
                      (String) A link to for the dashboard logo.
                      A link to for the dashboard logo.
                    type: string
                  logo:
                    description: |-
                      (String) A URL pointing to an image file to use for the dashboard logo.
                      A URL pointing to an image file to use for the dashboard logo.
                    type: string
                  paginate:
                    description: |-
                      (Boolean) Determines if pagination is on or off. (Default true).
                      Determines if pagination is on or off. (Default `true`).
                    type: boolean
                  paginationRate:
                    description: |-
                      (Number) How often to trigger pagination in seconds. Possible values 30, 60 and 300. (Default 60).
                      How often to trigger pagination in seconds. Possible values `30`, `60` and `300`. (Default `60`).
                    type: number
                  refreshRate:
                    description: |-
                      (Number) How often to refresh the dashboard in seconds. Possible values 60, '300' and 600. (Default 60).
                      How often to refresh the dashboard in seconds. Possible values `60`, '300' and `600`. (Default `60`).
                    type: number
                  showCheckRunLinks:
                    description: |-
                      (Boolean) Show or hide check run links on the dashboard. (Default false).
                      Show or hide check run links on the dashboard. (Default `false`).
                    type: boolean
                  showHeader:
                    description: |-
                      (Boolean) Show or hide header and description on the dashboard. (Default true).
                      Show or hide header and description on the dashboard. (Default `true`).
                    type: boolean
                  showP95:
                    description: |-
                      (Boolean) Show or hide the P95 stats on the dashboard. (Default true).
                      Show or hide the P95 stats on the dashboard. (Default `true`).
                    type: boolean
                  showP99:
                    description: |-
                      (Boolean) Show or hide the P99 stats on the dashboard. (Default true).
                      Show or hide the P99 stats on the dashboard. (Default `true`).
                    type: boolean
                  tags:
                    description: |-
                      (Set of String) A list of one or more tags that filter which checks to display on the dashboard.
                      A list of one or more tags that filter which checks to display on the dashboard.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  useTagsAndOperator:
                    description: |-
                      (Boolean) Set when to use AND operator for fetching dashboard tags. (Default false).
                      Set when to use AND operator for fetching dashboard tags. (Default `false`).
                    type: boolean
                  width:
                    description: |-
                      (String) Determines whether to use the full screen or focus in the center. Possible values are FULL and 960PX. (Default FULL).
                      Determines whether to use the full screen or focus in the center. Possible values are `FULL` and `960PX`. (Default `FULL`).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.customUrl is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.customUrl)
                || (has(self.initProvider) && has(self.initProvider.customUrl))'
            - message: spec.forProvider.header is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.header)
                || (has(self.initProvider) && has(self.initProvider.header))'
          status:
            description: DashboardStatus defines the observed state of Dashboard.
            properties:
              atProvider:
                properties:
                  checksPerPage:
                    description: |-
                      (Number) Determines how many checks to show per page. Possible values are between 1 and 20. (Default 15).
                      Determines how many checks to show per page. Possible values are between 1 and 20. (Default `15`).
                    type: number
                  customCss:
                    description: |-
                      (String) Custom CSS to be applied to the dashboard.
                      Custom CSS to be applied to the dashboard.
                    type: string
                  customDomain:
                    description: |-
                      (String) A custom user domain, e.g. 'status.example.com'. See the docs on updating your DNS and SSL usage.
                      A custom user domain, e.g. 'status.example.com'. See the docs on updating your DNS and SSL usage.
                    type: string
                  customUrl:
                    description: |-
                      (String) A subdomain name under 'checklyhq.com'. Needs to be unique across all users.
                      A subdomain name under 'checklyhq.com'. Needs to be unique across all users.
                    type: string
                  description:
                    description: |-
                      (String) HTML  description for the dashboard.
                      HTML <meta> description for the dashboard.
                    type: string
                  enableIncidents:
                    description: |-
                      (Boolean) Enable incident support for the dashboard. (Default false).
                      Enable incident support for the dashboard. (Default `false`).
                    type: boolean
                  expandChecks:
                    description: |-
                      (Boolean) Expand or collapse checks on the dashboard. (Default false).
                      Expand or collapse checks on the dashboard. (Default `false`).
                    type: boolean
                  favicon:
                    description: |-
                      (String) A URL pointing to an image file to use as browser favicon.
                      A URL pointing to an image file to use as browser favicon.
                    type: string
                  header:
                    description: |-
                      (String) A piece of text displayed at the top of your dashboard.
                      A piece of text displayed at the top of your dashboard.
                    type: string
                  hideTags:
                    description: |-
                      (Boolean) Show or hide the tags on the dashboard. (Default false).
                      Show or hide the tags on the dashboard. (Default `false`).
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isPrivate:
                    description: |-
                      (Boolean) Set your dashboard as private and generate key.
                      Set your dashboard as private and generate key.
                    type: boolean
                  link:
                    description: |-
                      (String) A link to for the dashboard logo.
                      A link to for the dashboard logo.
                    type: string
                  logo:
                    description: |-
                      (String) A URL pointing to an image file to use for the dashboard logo.
                      A URL pointing to an image file to use for the dashboard logo.
                    type: string
                  paginate:
                    description: |-
                      (Boolean) Determines if pagination is on or off. (Default true).
                      Determines if pagination is on or off. (Default `true`).
                    type: boolean
                  paginationRate:
                    description: |-
                      (Number) How often to trigger pagination in seconds. Possible values 30, 60 and 300. (Default 60).
                      How often to trigger pagination in seconds. Possible values `30`, `60` and `300`. (Default `60`).
                    type: number
                  refreshRate:
                    description: |-
                      (Number) How often to refresh the dashboard in seconds. Possible values 60, '300' and 600. (Default 60).
                      How often to refresh the dashboard in seconds. Possible values `60`, '300' and `600`. (Default `60`).
                    type: number
                  showCheckRunLinks:
                    description: |-
                      (Boolean) Show or hide check run links on the dashboard. (Default false).
                      Show or hide check run links on the dashboard. (Default `false`).
                    type: boolean
                  showHeader:
                    description: |-
                      (Boolean) Show or hide header and description on the dashboard. (Default true).
                      Show or hide header and description on the dashboard. (Default `true`).
                    type: boolean
                  showP95:
                    description: |-
                      (Boolean) Show or hide the P95 stats on the dashboard. (Default true).
                      Show or hide the P95 stats on the dashboard. (Default `true`).
                    type: boolean
                  showP99:
                    description: |-
                      (Boolean) Show or hide the P99 stats on the dashboard. (Default true).
                      Show or hide the P99 stats on the dashboard. (Default `true`).
                    type: boolean
                  tags:
                    description: |-
                      (Set of String) A list of one or more tags that filter which checks to display on the dashboard.
                      A list of one or more tags that filter which checks to display on the dashboard.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  useTagsAndOperator:
                    description: |-
                      (Boolean) Set when to use AND operator for fetching dashboard tags. (Default false).
                      Set when to use AND operator for fetching dashboard tags. (Default `false`).
                    type: boolean
                  width:
                    description: |-
                      (String) Determines whether to use the full screen or focus in the center. Possible values are FULL and 960PX. (Default FULL).
                      Determines whether to use the full screen or focus in the center. Possible values are `FULL` and `960PX`. (Default `FULL`).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
